spring.application.name=backend

# ==========================================
#  HotelApp Backend - application.properties
# ==========================================

# Base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=C0n0c3r 4 5kr1ll3x
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Servidor
server.port=8080

# JWT
jwt.secret=HotelAppSuperSecretKey2024XyZaBcDeFgHiJkLmNoPqRsTuVwXyZ
jwt.expiration=86400000

# ==========================================
#  Configuración de imágenes
# ==========================================

# Carpeta donde se guardan los archivos subidos
# Se crea automáticamente en la raíz del proyecto al iniciar
storage.upload-dir=uploads

# URL base del servidor ? CAMBIA LA IP por la de tu máquina (ipconfig en CMD)
# Esta URL se agrega al frente de imagen_url para que Flutter cargue las fotos
app.base-url=http://192.168.8.2:8080

# Tamaño máximo de archivos subidos (10MB por archivo, 20MB por request)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
